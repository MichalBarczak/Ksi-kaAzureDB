@model Modele.ElementZamowienia

@{
    ViewData["Title"] = "Szczegóły";
}

<h1>Szczegóły</h1>

<div>
    <h4>Elementy Zamówienia</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IlośćProduktu)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IlośćProduktu)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edycja</a> |
    <a asp-action="Index">Powrót do listy</a>
</div>
